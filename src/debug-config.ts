// Set to false before production deploy to strip all debug features
export const DEBUG_MODE = true

// Initial state of the debug visual overlay (D key toggles at runtime)
// false = debug dots are hidden on startup
export const DEBUG_VISUALS = false
